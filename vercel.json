{
  "buildCommand": "cd react-app && npm install && npm run build && node -e \"const fs=require('fs'); fs.cpSync('react-app/dist', 'dist', {recursive: true});\"",
  "outputDirectory": "dist",
  "framework": "vite"
}
