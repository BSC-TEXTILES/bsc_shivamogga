{
  "name": "bsc-shivamogga-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node build.cjs",
    "build": "node build.cjs",
    "dev": "npm --prefix react-app run dev",
    "preview": "npm --prefix react-app run preview"
  }
}
