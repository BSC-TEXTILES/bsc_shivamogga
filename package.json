{
  "name": "bsc-shivamogga-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd react-app && npm install",
    "build": "cd react-app && npm install && npm run build && node -e \"const fs=require('fs'); fs.cpSync('dist', '../dist', {recursive: true});\"",
    "dev": "npm --prefix react-app run dev",
    "preview": "npm --prefix react-app run preview"
  }
}
